{
  "name": "qiniu-ime",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --dir app/web dev",
    "build:web": "pnpm --dir app/web build",
    "test:web": "pnpm --dir app/web test -- --coverage",
    "test:e2e": "pnpm --dir app/web exec playwright test",
    "install:browsers": "pnpm --dir app/web exec playwright install chromium"
  },
  "packageManager": "pnpm@10.0.0"
}

